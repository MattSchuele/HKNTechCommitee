# Site settings
title: Eta Kappa Nu Alpha Chapter
baseurl: "" # the subpath of your site, e.g. /blog/

# Build settings
markdown: kramdown

collections:
  - about
  - service
  - initiates
  - corporate
  - main

exclude: ['Gemfile', 'Gemfile.lock', 'config.ru', 'vendor', 'script', 'Rake', '.travis.yml']

# gems: [jekyll-contentblocks]
